<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KYCApp.Views.ResumenPage"
             Title="Resumen Final"
             BackgroundColor="#000000">

    <ScrollView>
        <StackLayout Spacing="0">
            
            <!-- Header con gradiente Galxe -->
            <Frame HasShadow="False"
                   CornerRadius="0"
                   Padding="25,50,25,30">
                <Frame.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#8B5CF6" Offset="0.0" />
                        <GradientStop Color="#3B82F6" Offset="1.0" />
                    </LinearGradientBrush>
                </Frame.Background>
                <StackLayout Orientation="Horizontal">
                    <Button Text="✕"
                            FontSize="24"
                            TextColor="White"
                            BackgroundColor="Transparent"
                            WidthRequest="40"
                            HeightRequest="40"
                            CornerRadius="20"
                            Clicked="OnBackClicked"
                            VerticalOptions="Center" />
                    
                    <Label Text="📋 Resumen Final" 
                           FontSize="24" 
                           FontAttributes="Bold"
                           FontFamily="Arial"
                           TextColor="White" 
                           VerticalOptions="Center" 
                           HorizontalOptions="CenterAndExpand" />
                    
                    <Button Text="⋮"
                            FontSize="24"
                            TextColor="White"
                            BackgroundColor="Transparent"
                            WidthRequest="40"
                            HeightRequest="40"
                            VerticalOptions="Center" />
                </StackLayout>
            </Frame>
            
            <!-- Contenido principal -->
            <StackLayout Padding="20" Spacing="25" BackgroundColor="#000000">
                
                <!-- Tarjeta de información del visitante -->
                <Frame BackgroundColor="#1A1A1A" 
                       CornerRadius="16"
                       Padding="25,30"
                       HasShadow="True"
                       BorderColor="#333333">
                    <StackLayout Spacing="20">
                        
                        <!-- Header de la tarjeta -->
                        <StackLayout Orientation="Horizontal" Spacing="15">
                            <Label Text="👤" FontSize="24" VerticalOptions="Center" />
                            <StackLayout VerticalOptions="Center">
                                <Label Text="Información del Visitante" 
                                       FontSize="18" 
                                       FontAttributes="Bold"
                                       FontFamily="Arial"
                                       TextColor="#FFFFFF" />
                                <Label Text="Datos verificados del registro" 
                                       FontSize="12" 
                                       FontFamily="Arial"
                                       TextColor="#9CA3AF" />
                            </StackLayout>
                        </StackLayout>
                        
                        <!-- Datos del visitante -->
                        <StackLayout Spacing="15">
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Label Text="🔸" FontSize="12" TextColor="#8B5CF6" VerticalOptions="Center" />
                                <Label Text="Código QR:" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       FontAttributes="Bold"
                                       TextColor="#E0E0E0"
                                       VerticalOptions="Center" />
                                <Label x:Name="QRCodeLabel"
                                       Text="[QR_CODE]" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       TextColor="#8B5CF6"
                                       VerticalOptions="Center" />
                            </StackLayout>
                            
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Label Text="🔸" FontSize="12" TextColor="#8B5CF6" VerticalOptions="Center" />
                                <Label Text="Nombre:" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       FontAttributes="Bold"
                                       TextColor="#E0E0E0"
                                       VerticalOptions="Center" />
                                <Label x:Name="NombreLabel"
                                       Text="[NOMBRE]" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       TextColor="#FFFFFF"
                                       VerticalOptions="Center" />
                            </StackLayout>
                            
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Label Text="🔸" FontSize="12" TextColor="#8B5CF6" VerticalOptions="Center" />
                                <Label Text="Empresa:" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       FontAttributes="Bold"
                                       TextColor="#E0E0E0"
                                       VerticalOptions="Center" />
                                <Label x:Name="EmpresaLabel"
                                       Text="[EMPRESA]" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       TextColor="#FFFFFF"
                                       VerticalOptions="Center" />
                            </StackLayout>
                            
                            <StackLayout Orientation="Horizontal" Spacing="10">
                                <Label Text="🔸" FontSize="12" TextColor="#8B5CF6" VerticalOptions="Center" />
                                <Label Text="Área a visitar:" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       FontAttributes="Bold"
                                       TextColor="#E0E0E0"
                                       VerticalOptions="Center" />
                                <Label x:Name="AreaLabel"
                                       Text="[AREA]" 
                                       FontSize="13" 
                                       FontFamily="Arial"
                                       TextColor="#FFFFFF"
                                       VerticalOptions="Center" />
                            </StackLayout>
                        </StackLayout>
                    </StackLayout>
                </Frame>
                
                <!-- Tarjeta de imágenes capturadas -->
                <Frame BackgroundColor="#1A1A1A" 
                       CornerRadius="16"
                       Padding="25,30"
                       HasShadow="False"
                       BorderColor="#333333">
                    <StackLayout Spacing="20">
                        
                        <!-- Header de imágenes -->
                        <StackLayout Orientation="Horizontal" Spacing="15">
                            <Label Text="📷" FontSize="24" VerticalOptions="Center" />
                            <StackLayout VerticalOptions="Center">
                                <Label Text="Imágenes Capturadas" 
                                       FontSize="18" 
                                       FontAttributes="Bold"
                                       FontFamily="Arial"
                                       TextColor="#FFFFFF" />
                                <Label Text="Documentación fotográfica" 
                                       FontSize="12" 
                                       FontFamily="Arial"
                                       TextColor="#9CA3AF" />
                            </StackLayout>
                        </StackLayout>
                        
                        <!-- Grid de imágenes -->
                        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*" RowSpacing="15">
                            
                            <!-- Documento -->
                            <Frame Grid.Row="0" Grid.Column="0"
                                   BackgroundColor="#2A2A2A"
                                   CornerRadius="12"
                                   Padding="15"
                                   HasShadow="False">
                                <StackLayout Spacing="10">
                                    <Label Text="📄" FontSize="32" HorizontalOptions="Center" TextColor="#8B5CF6" />
                                    <Label Text="Documento" 
                                           FontSize="12" 
                                           FontFamily="Arial"
                                           FontAttributes="Bold"
                                           TextColor="#FFFFFF"
                                           HorizontalOptions="Center" />
                                    <Image x:Name="DocumentoImagen" 
                                           HeightRequest="80"
                                           Aspect="AspectFill" />
                                </StackLayout>
                            </Frame>
                            
                            <!-- Placa -->
                            <Frame Grid.Row="1" Grid.Column="0"
                                   BackgroundColor="#2A2A2A"
                                   CornerRadius="12"
                                   Padding="15"
                                   HasShadow="False">
                                <StackLayout Spacing="10">
                                    <Label Text="🚗" FontSize="32" HorizontalOptions="Center" TextColor="#8B5CF6" />
                                    <Label Text="Placa Vehicular" 
                                           FontSize="12" 
                                           FontFamily="Arial"
                                           FontAttributes="Bold"
                                           TextColor="#FFFFFF"
                                           HorizontalOptions="Center" />
                                    <Image x:Name="PlacaImagen" 
                                           HeightRequest="60"
                                           Aspect="AspectFit" />
                                </StackLayout>
                            </Frame>
                        </Grid>
                    </StackLayout>
                </Frame>
                
                <!-- Botón de confirmación final -->
                <Button Text="✅ Confirmar Registro"
                        FontSize="18"
                        FontAttributes="Bold"
                        FontFamily="Arial"
                        TextColor="White"
                        CornerRadius="12"
                        HeightRequest="55"
                        Clicked="OnConfirmClicked"
                        Margin="0,10,0,30">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#059669" Offset="0.0" />
                            <GradientStop Color="#047857" Offset="1.0" />
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
                
            </StackLayout>
        </StackLayout>
    </ScrollView>

</ContentPage>
