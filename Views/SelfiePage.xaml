<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KYCApp.Views.SelfiePage"
             Title="Capturar Selfie">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" 
               Text="Toma una selfie" 
               FontSize="20"
               HorizontalOptions="Center"
               Margin="20" />

        <Frame Grid.Row="1" 
               BackgroundColor="LightGray"
               Margin="20"
               HasShadow="True">
            <Grid>
                <Label Text="Tu selfie aparecerá aquí"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       x:Name="PlaceholderLabel" />
                <Image x:Name="SelfieImage"
                       IsVisible="False"
                       Aspect="AspectFit" />
            </Grid>
        </Frame>

        <StackLayout Grid.Row="2" 
                     Padding="20">
            <Button Text="Tomar Selfie"
                    BackgroundColor="#007ACC"
                    TextColor="White"
                    FontSize="18"
                    Clicked="OnTakeSelfieClicked"
                    x:Name="TakeSelfieButton" />
            
            <Button Text="Finalizar"
                    BackgroundColor="#28a745"
                    TextColor="White"
                    FontSize="18"
                    Clicked="OnFinishClicked"
                    IsVisible="False"
                    x:Name="FinishButton"
                    Margin="0,10,0,0" />
        </StackLayout>
    </Grid>

</ContentPage>
